# Development/verification tools on top of requirements.txt.
# Optional image-analysis extras for dev_probe live in tools/requirements-dev.txt.
-r requirements.txt
pytest>=8.0     # tests/
Pillow>=10.0    # tools/dev_probe.py: blank-frame detection, contact sheet, GIF
maturin>=1.9    # builds rust_core (see rust_core/README.md; needs rustup + MSVC Build Tools)
